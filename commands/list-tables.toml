name = "list-tables"
description = "List all tables in the PostgreSQL database."

prompt = """
List all tables in the PostgreSQL database using the list_tables tool.
Format the results as a clean list of schema.table names.
"""

[parameters]
